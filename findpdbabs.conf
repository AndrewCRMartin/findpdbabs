faadir='/serv/data/pdbseq'  # Directory of all PDB sequences
abpdbdir="${HOME}/git/abymod/DATA/abpdblib/"  # Directory of antibody PDB files

# Don't need to change anything below here
dbmfile='./findpdbabs.dbm'  # DBM file to record what we have already looked at
seqfile='sequences.faa'     # All new sequences in the PDB concatenated
tplsubdir='templates'       # Directory containing template sequences for antibodies
tcrabsfile='tcrabs.faa'     # Fasta file of template antibody and TCR sequences
tcrseqsfile='tcrseqs.faa'   # Non-redundant TCR sequences file
